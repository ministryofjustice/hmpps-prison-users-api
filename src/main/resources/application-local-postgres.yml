hmpps-auth:
  url: "http://localhost:9090/auth"

spring:
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  datasource:
    url: jdbc:postgresql://localhost:5434/prison-users-api-db
    username: admin
    password: admin_password

  h2:
    console:
      enabled: false
